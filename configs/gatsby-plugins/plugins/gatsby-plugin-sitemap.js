/**
 * @description Generate Sitemap
 * @name Sitemap Gatsby Plugin
 * @link https://www.gatsbyjs.com/plugins/gatsby-plugin-sitemap/
 * @extra output (string = /sitemap) Folder path where sitemaps are stored.
 */

const purgeCssConfig = {
  resolve: `gatsby-plugin-sitemap`,
};

module.exports = purgeCssConfig;
